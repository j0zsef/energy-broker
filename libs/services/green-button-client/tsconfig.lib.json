{
  "extends": "../../../tsconfig.project.json",
  "compilerOptions": {
    // outDir should be local to the project and not in the same folder as any other tsconfig.*.json
    "outDir": "../../../dist/out-tsc/libs/services/api-client",
    "tsBuildInfoFile": "../../../dist/out-tsc/libs/services/api-client/tsconfig.lib.tsbuildinfo",
    "types": ["node"]
    // Any overrides
  },
  "include": ["src/**/*.ts"],
  "exclude": [
    // exclude config and test files
  ],
  "references": [
    {
      "path": "../../shared/tsconfig.lib.json"
    }
  ]
}
